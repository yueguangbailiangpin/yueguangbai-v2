# 月光白 V2 项目说明

## 1. 项目目标

月光白 V2 用于统一管理卖家客户、买家客户、店铺、产品、需求批次、预约、订单、评论审核、买家返款、卖家结算、内部结算、图片和内部任务。

系统目标是替代“多张协作表格互相复制”的运行方式，同时保留清晰的数据权威和人工沟通边界：

- 私人微信可继续承担买家、卖家的日常沟通、提醒和关系维护；
- Buyer / Seller Web 门户承担客户正式提交、查询和业务操作；
- Staff 通过 Cloudflare Access 验证邮箱身份，并在月光白 Staff Web/API 完成正式内部操作；
- D1 是唯一权威业务事实来源；
- R2 是正式在线图片来源；
- 飞书已退出当前及计划运行架构，不再承担员工登录、业务同步、任务镜像、回调或告警运行能力。

## 2. 当前技术路线

```text
私人微信：买家 / 卖家的日常沟通、提醒、关系维护
Buyer / Seller Web：客户正式业务入口
Cloudflare Access：Staff 邮箱身份验证
Staff Web：内部受控工作台和正式业务动作
Workers + Hono：API、权限、状态机、幂等、调度与审计
D1：唯一权威业务数据库
R2：唯一正式在线图片源
```

Cloudflare Access 只证明 Staff 邮箱身份；员工是否可用、唯一角色、Marketplace 范围以及个人授权 / 禁用由 D1 权限模型最终决定。

私人微信聊天不是正式业务事实。历史飞书 Migration / archived Change 只用于升级和审计，不构成现行运行能力。

## 3. V2 产品范围

V2 的目标范围包含：

1. Staff 邮箱身份验证、内部账号、角色和细粒度权限。
2. 买家、卖家组织、卖家成员、渠道和编号体系。
3. 店铺、产品、产品版本和产品申请。
4. 需求批次创建 / 发布、买家可见需求和预约。
5. 预约预检、售前审核和容量 / 并发控制。
6. 买家提交待核对订单资料，售前确认后生成正式订单与业务快照。
7. 订单号唯一认领、买家编号、汇率及商业快照。
8. Buyer / Seller / Staff Web 门户。
9. 评论资料提交与审核。
10. 买家返款、卖家本金 / 服务费、内部结算、冲正和财务查询。
11. Staff 内部任务、队列、领取 / 分派、调度、异常与审计能力。
12. R2 图片上传、校验、私有读取、状态机和失败补偿。
13. 幂等、版本控制、审计、Outbox、备份 / 恢复门禁、匿名测试和上线验收。

“属于产品范围”不等于“已经生产验收完成”。当前实现和验收状态以 `docs/CURRENT_SYSTEM_STATE.md`、Acceptance Matrix、当前 OpenSpec 与真实测试结果为准。

## 4. V2 明确不做 / 默认不启用

- 企业微信或 CloudBase 作为核心架构。
- Google Chat 作为核心业务入口。
- 私人微信自动化、Hook、RPA、机器人操控。
- 全量微信聊天导入。
- 门户完整多轮客服聊天。
- 飞书作为 Staff 登录、正式业务数据库、任务同步或业务运行时。
- Google Drive 作为在线图片权威回源。
- 自动发布、修改或删除 Amazon 评论。
- 未经明确授权自动创建、修改或部署远程资源。

## 5. 核心业务阶段

### A. 主数据与身份

Buyer、Seller、Staff、组织、成员、渠道、编号、店铺、产品、角色和权限。

### B. 产品、需求与预约

产品申请 → Staff 审核 → 需求批次 → 发布 → 买家查看 → 预约 / 预检 / 审批。

### C. 订单

待核对订单资料 → 售前确认 → 正式订单 → 订单与商业快照 → 后续状态控制。

### D. 评论与财务

评论提交 / 审核 → 买家返款 → 卖家应收 / 结算 → 内部结算 → 冲正 / 财务查询 / 审计。

### E. 文件、调度与运营保障

R2 文件链、任务 / Scheduler、Outbox、审计、备份恢复、异常处理、上线门禁。

## 6. 数据与集成原则

- D1 / R2 承担正式业务事实，外部协作工具不得成为第二套真相。
- 外部 Provider、归档或辅助工具只有在明确产品决策、OpenSpec 和配置评审后才能进入运行路径。
- 已完成订单与财务事实不得用普通覆盖式更新改写；更正必须通过版本、冲正、补充事实和审计处理。
- 关键写操作必须遵守权限、幂等、版本 / 并发控制和审计要求。

## 7. 旧仓库复用边界

旧仓库：

```text
yinghua520ai/yueguangbai-ChatGPT
Commit: e211dff657dbcb100b111ba69a75f8e51268aef3
Tree:   f8c8c1fe2312322b49c09fd1d98638a780a769da
```

只允许按 `docs/migration/V2_LEGACY_CODE_REUSE.md` 提取已批准的小型工具、算法和测试思想。不得迁入旧 `.git`、旧 Migration、旧真实数据、旧 Cloudflare 资源标识或旧财务覆盖式实现。
