# Tasks: Rakuten and TikTok Japan Real Adapter Preparation

## Governance and Sources

- [x] 1.1 Verify remote, complete baseline SHA, main dirty-state ownership and independent worktree/branch before edits.
- [x] 1.2 Inventory the existing registry, identifiers, platform carriers, permission/finance/audit boundaries and production adapter patterns.
- [x] 1.3 Record only official Rakuten/TikTok facts, marking login-gated or unstated details `UNKNOWN/BLOCKED`.
- [x] 1.4 Freeze `NO_SCHEMA_CHANGE`, zero external writes and truthful unavailable production status in OpenSpec before code.

## Contracts and Adapters

- [x] 2.1 Add bounded read-only marketplace Provider contracts, safe DTOs, opaque pagination and stable error classes.
- [x] 2.2 Add TikTok official request signing, pinned read-only order/product queries, bounded transport, retry classification and runtime parsing.
- [x] 2.3 Add TikTok webhook raw-byte HMAC verifier and official golden-vector tests without registering a route.
- [x] 2.4 Add Rakuten always-unavailable and deterministic fake read adapters; prove unavailable means no external call.
- [x] 2.5 Preserve `R-1`/`S-1`, TikTok long decimal order identifiers and `tiktokDLP2555Q` without Amazon validation.

## Preflight and Verification

- [x] 3.1 Add a repository-external, redacted activation manifest preflight and owner/platform checklist with zero-call counters.
- [x] 3.2 Add a static verifier for unchanged registry/routes/scheduler/templates/migrations and forbidden platform write methods.
- [x] 3.3 Run focused tests, package typechecks, OpenSpec strict, migration guards, security scan and the complete repository gate; preserve real PASS/FAIL/SKIP.

## Handoff

- [x] 4.1 Commit only the scoped worktree diff; do not push, open a PR, merge, deploy, migrate production or write any external resource.
- [x] 4.2 Report baseline/worktree/branch/commit, diff, Migration decision, commands/results, blockers, owner steps and exact external-write counters; stop at total-control review.
