# Tasks: Staff MCP and AI Production Enablement Preparation

## 0. Governance and Baseline

- [x] 0.1 Verify remote, exact `origin/main` SHA, main worktree status and isolated branch/worktree before modification.
- [x] 0.2 Read AGENTS, decision/product rules, AI governance, OpenSpec config, canonical/archived Staff MCP specifications, contracts, runbooks, acceptance evidence and relevant Cloudflare/MCP primary documentation.
- [x] 0.3 Inventory baseline capability and record existing timeout/redirect/non-JSON/replay/audit-failure coverage instead of duplicating it.

## 1. Migration and Contract

- [x] 1.1 Record `NO_SCHEMA_CHANGE` and preserve Migration 0038 ownership.
- [x] 1.2 Freeze public metadata URLs, challenge scope, custom-domain alignment, explicit tool allowlist and Git-external activation-evidence contract.

## 2. Implementation

- [x] 2.1 Add exact same-origin documentation/policy configuration and RFC 9728 metadata fields.
- [x] 2.2 Add strict production enabled-tool parsing and fail-closed catalog composition while preserving the two unavailable projections.
- [x] 2.3 Extend zero-network preflight for domain/public metadata/tool gate and client-registration evidence.
- [x] 2.4 Add disabled template placeholders and a non-secret example activation-evidence document.

## 3. Tests

- [x] 3.1 Test metadata/challenge output and missing/unsafe public URL configuration.
- [x] 3.2 Test exact enabled-tool subsets plus missing, duplicate, unknown and unavailable tool failures.
- [x] 3.3 Test custom-domain, registration mode, redirect, PKCE and catalog evidence drift.
- [x] 3.4 Re-run existing timeout, redirect, non-JSON, replay and audit-failure tests through the targeted production check.

## 4. Runbook, Privacy and Rollback

- [x] 4.1 Document minimum owner accounts, custom domain, public policy pages, managed Secrets, client registration/redirect steps and staged tool rollout.
- [x] 4.2 Document validation order, zero-network preflight status semantics and rollback without down-migration.
- [x] 4.3 Refresh acceptance evidence with actual commands, external blockers and zero external writes.

## 5. Acceptance and Handoff

- [x] 5.1 Run target and all-Change strict OpenSpec validation.
- [x] 5.2 Run targeted Staff MCP production gates, typecheck/security/dependency/full repository gates proportionate to the final diff.
- [x] 5.3 Review final diff, commit only scoped files, confirm no main-worktree/user-directory impact and report `PRODUCTION_NO_GO` pending total-control review.
