# Tasks: stage7f-visual-evidence-fixture-repair

## Fixed boundary

- [x] Confirm the fixed checkout, branch, HEAD, clean-start state, upstream-ahead count, and the four expected pre-existing edited files.
- [x] Read the repository instructions, parent Stage 7F OpenSpec, current Stage 7 handoff, current Staff/Review sources, and existing visual harnesses before editing.
- [x] Reproduce the stale baseline and preserve its direct exit evidence.

## Fixture and harness repair

- [x] Update Staff visual waits, role permissions/labels, and the current lean Dashboard fixture.
- [x] Add strict Staff order-evidence preflight and controlled image read fixtures.
- [x] Align contacts and settlement fixture endpoint matching, current strict response shapes, and screenshot normal-state checks.
- [x] Add the three current Review access-management GET demo responses.
- [x] Apply and verify the narrowly scoped Dashboard 44px control rule without lowering the assertion.

## Evidence

- [x] Add the dedicated 17-Staff/4-Review evidence spec with semantic readiness, forbidden-state, image-decode, overflow, and real-drawer assertions.
- [x] Generate all 21 named screenshots from the local built Review runtime.
- [x] Manually inspect every generated screenshot and record the result in `evidence.md`.

## Verification and closure

- [x] Run focused Playwright suites with direct exits.
- [x] Run `npm run typecheck`, `npm test`, `npm run build`, `npm run check`, CSS ownership/duplicate gates, web-source/static boundary gates, API contract gate, OpenSpec strict validation, and `git diff --check`.
- [x] Reconfirm start/end HEAD, the final diff scope, no remote/production action, and the 21-view manifest.
- [x] Update the parent Stage 7F `tasks.md`/`evidence.md` and Stage 7 handoff only after all required evidence and local gates pass.
- [x] Create one atomic local commit; do not push, deploy, sync, or archive any Change.
