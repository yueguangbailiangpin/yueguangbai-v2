# Tasks: Canonical product evidence alignment

## Migration

- [x] M1. Confirm no Migration is required and preserve every historical Migration byte.
- [x] M2. Confirm no production migration or remote database operation is in scope.

## Governance and OpenSpec

- [x] G1. Read and apply `AGENTS.md`, OpenSpec config, AI engineering governance, Frozen Product Baseline, and Decision Register authority order.
- [x] G2. Create one active `canonical-product-evidence-alignment` Change covering the five requested capabilities.
- [x] G3. Add superseding D-033 and D-034 without rewriting D-024 or D-025.
- [x] G4. Record the Buyer canonical model, Staff five-role model, Admin frontend boundary, and Seller settlement workflow gap.
- [x] G5. Controller authorization for Formal Verify, sync and archive is recorded in the 2026-08-12 task; Formal Verify is in progress, and this task does not claim sync or archive completion.

## Verifier evidence

- [x] V1. Run `verify-module1-buyer-formal.mjs` once and retain its 58/58, 116/116 PASS as archived completeness evidence.
- [x] V2. Run `verify-staff-acquisition-funnel.mjs` once and preserve its pre-existing runtime/spec conflict as a blocker.
- [x] V3. Compute the current route inventory with the API contract baseline test and document 237 as the authoritative current count.
- [x] V4. Replace the scheduling verifier's stale 180 assertion with the canonical runtime-backed API route-baseline test, without changing route registration or storing a second count.
- [x] V5. Run `verify-admin-business-dashboard.mjs` once and classify it as backend/no-schema-change evidence, not canonical frontend proof.
- [x] V6. Resolve the acquisition `channel_id` runtime/spec conflict through the separately approved `acquisition-source-authority-alignment` Change and D-035; this canonical Change does not alter business behavior.

## Node safety

- [x] N1. Run the OAuth PKCE Node safety test directly: 6/6 passed.
- [x] N2. Run the redacted D1 export Node safety test directly: 3/3 passed.
- [x] N3. Add the minimal `test:node-safety` script without changing either Node safety test.
- [x] N4. Invoke the gate near the start of `check` and rerun it before full local acceptance.

## Preservation checks

- [x] P1. Preserve old Buyer Dashboard runtime/test files and manifest fetch script.
- [x] P2. Preserve Staff/Seller settlement runtime and MSW evidence, Acquisition workbench evidence, and Admin legacy consumer evidence.
- [x] P3. Preserve API/contracts, Domain, Migration, production configuration, and remote state.
