# Tasks: Post-M16 Static Verifier Alignment

## 1. Freeze and Reproduce

- [x] 1.1 Freeze this independent Change with explicit no-production/no-Migration scope.
- [x] 1.2 Reproduce the five named verifier failures on the current branch and record the exact commands/results.

## 2. Targeted Static Verifier Alignment

- [x] 2.1 Align the Wave 14A Seller exact-label assertion with current production source.
- [x] 2.2 Align the four-role verifier with its owned `0035` prefix and current governed chain.
- [x] 2.3 Align the acquisition verifier with its owned `0036` Migration and current chain.
- [x] 2.4 Align the scheduling verifier with current lazy Staff route modules.
- [x] 2.5 Align the dashboard no-schema-change verifier with M16 `0037` ownership and current chain.
- [x] 2.6 Add or update focused verifier regression coverage without weakening failures.

## 3. Validation and Handoff

- [x] 3.1 Run each affected verifier and migration guards after the targeted edits.
- [x] 3.2 Run target/all strict OpenSpec validation, diff/secret/dependency checks, and scope review.
- [x] 3.3 Run one complete `npm run check` and record every actual result.
- [x] 3.4 Run a read-only Ponytail minimality review only after all validation completes; make no changes from it.
- [x] 3.5 Stop unstaged, uncommitted, unpushed, unarchived, with no PR, deployment, or external-resource activity.
