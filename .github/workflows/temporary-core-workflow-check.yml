name: Temporary core workflow check

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npx vitest run apps/api/src/reservations/reservations.test.ts apps/api/src/product-applications/product-applications.test.ts apps/api/src/order-instructions apps/web/src/staff
